const PUBLIC_PAGES = {
  login: '/login',
}

const PRIVATE_PAGES = {
  root: '/',
  settings: '/settings',
  swiping: '/swiping',
}

export const PATH_PAGE = {
  ...PUBLIC_PAGES,
  ...PRIVATE_PAGES,
}
